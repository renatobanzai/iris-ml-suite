Class App.Installer
{

XData setup
{
<Manifest>
  <Default Name="SourceDir" Value="#{$system.Process.CurrentDirectory()}src"/>
  <Default Name="Namespace" Value="PYTHON"/>
  <Default Name="NewDataDir" Value="#{$system.Process.CurrentDirectory()}"/>
  <Namespace Name="${Namespace}" Create="no">
    <Import File="${SourceDir}" Flags="ck" Recurse="1"/>
  </Namespace>
  <RunInstall Class="App.Installer" Method="DefaultNS"/>
  <Invoke Class="App.Installer" Method="BuildIndex" CheckStatus="1"/>
  <RunInstall Class="App.Installer" Method="IngestData"/>
</Manifest>
}

ClassMethod setup(ByRef pVars, pLogLevel As %Integer = 3, pInstaller As %Installer.Installer, pLogger As %Installer.AbstractLogger) As %Status [ CodeMode = objectgenerator, Internal ]
{
  #; Let XGL document generate code for this method. 
  Quit ##class(%Installer.Manifest).%Generate(%compiledclass, %code, "setup")
}

ClassMethod DefaultNS(pVars, pLogLevel, tInstaller) As %Status
{
	Set Namespace = tInstaller.Evaluate("${Namespace}")
	Do tInstaller.PushNS("%SYS")
	For user = "UnknownUser", "Admin", "SuperUser", "_SYSTEM", $username {
		Set tProps("NameSpace") = Namespace
		Set tSC = ##class(Security.Users).Modify(user, .tProps)
	}
	Do tInstaller.PopNS()
	Quit tSC
}

ClassMethod BuildIndex() As %Status
{
  W "Building Indexes"
  Zn "PYTHON"
  Set tSC = ##class(community.post).%BuildIndices()
  Set tSC = ##class(community.tag).%BuildIndices()
  Set tSC = ##class(community.comment).%BuildIndices()
  Quit tSC
}

ClassMethod IngestData(pVars, pLogLevel, tInstaller) As %Status
{
  W "Ingesting Data"
  Zn "PYTHON"
  zpm "install csvgen"
  Set fytrain = tInstaller.Evaluate("${NewDataDir}") _ "ytrain.csv"
  Set fxtrain = tInstaller.Evaluate("${NewDataDir}") _ "xtrain.csv"
  Set fytest = tInstaller.Evaluate("${NewDataDir}") _ "ytest.csv"
  Set fxtest = tInstaller.Evaluate("${NewDataDir}") _ "xtest.csv"
  Set pclassytrain = "community.ytrain"
  Set pclassxtrain = "community.xtrain"
  Set pclassytest = "community.ytest"
  Set pclassxtest = "community.xtest"
  Set tSC = ##class(community.csvgen).Generate(fytrain,,.pclassytrain)
  Set tSC = ##class(community.csvgen).Generate(fxtrain,,.pclassxtrain)
  Set tSC = ##class(community.csvgen).Generate(fytest,,.pclassytest)
  Set tSC = ##class(community.csvgen).Generate(fxtest,,.pclassxtest)
  W "Creating Views"
  Set tSC = ##class(%SQL.Statement).%ExecDirect(, "CREATE VIEW community.Post_Train as SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%caché%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%microsoft windows server%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%deployment%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%node.js%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%.net%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%oauth2%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%monitoring%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%code snippet%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%article%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%high availability%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%github%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%eclipse%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%perforce%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%containerization%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%other%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%languages%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%xml%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%global summit 2016%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%callout%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%mirroring%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%ai%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%business rules%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%sharding%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%compatibility%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%document data model (nosql)%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%vue.js%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%big data experience%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%angular2%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%best practices%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%dashboards%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%alerts%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%video%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%big data%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%java%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%machine learning%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%debugging%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%compiler%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%architect%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%gcp%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%zen%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%databases%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%ecp%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%json%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%dtl%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%rest api%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%vscode%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%ui development%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%global masters%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%cloud%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%atelier 1.1 beta%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%failover%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%global summit%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%events%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%error handling%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%developer community%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%roles%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%analytics%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%git%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%management portal%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%intersystems data platform blog%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%backup%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%angular%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%azure%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%platforms%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%analyzer%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%online learning%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%hl7%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%c++%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%html%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%aws%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%apple macos%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%change management%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%cubes%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%global summit 2017%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%ldap%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%contest%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%global summit 2018%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%authorization%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%ensemble%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%testing%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%system administration%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%security%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%sql%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%red hat enterprise linux (rhel)%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%javascript%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%mapping%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%ssl%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%intersystems iris experience%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%tips & tricks%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%business operation%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%api%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%iris analytics (deepsee)%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%developer community faq%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%odbc%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%key value%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%dc feedback%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%tutorial%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%authentication%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%studio%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%atelier 1.2 beta%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%unstructured data%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%healthshare%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%soap%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%continuous delivery%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%intersystems business solutions and architectures%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%microservices%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%atelier 1.2%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%business process (bpl)%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%intersystems iris%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%performance%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%release%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%users%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%interoperability%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%open source%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%webinar%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%ftp%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%ifind%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%business service%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%journaling%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%mdx%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%beginner%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%tools%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%svn%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%documentation%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%iknow%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%python%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%jdbc%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%devops%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%mercurial%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%microsoft windows%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%iot%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%data import and export%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%suse linux%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%intersystems iris for health%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%graph%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%development environment%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%objectscript%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%visualization%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%atelier%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%worldwide response center (wrc)%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%globals%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%uima%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%distributed data management%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%continuous integration%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%frontend%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%docker%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%terminal%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%coding guidelines%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%issue tracking%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%field tests%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%namespace%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%csp%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%relational tables%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%indexing%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%java experience%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%data model%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%localization%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%application server%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%encryption%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%csv%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%intersystems official%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%react%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%open exchange%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%object data model%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%listings%'  union  SELECT top 20 id, Name, Tags, Text, PostType FROM Community.Post Where lang = 'en' and tags like '%oracle enterprise linux%'")
  Quit tSC
}



}
